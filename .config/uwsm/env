export XCURSOR_SIZE=24

# Machine-specific variables (gitignored, not synced) https://wiki.hypr.land/Nvidia/
# See env.d/env.example, copy to env.d/env

# Input methods https://fcitx-im.org/wiki/Using_Fcitx_5_on_Wayland
#export GTK_IM_MODULE=fcitx # Complained at the startup from fcitx maybe
export QT_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx
export SDL_IM_MODULE=fcitx
export GLFW_IM_MODULE=ibus
export INPUT_METHOD=fcitx

# QT
export QT_STYLE_OVERRIDE=kvantum

# Use Wayland preferred on all apps
export GDK_BACKEND=wayland,x11,*
export QT_QPA_PLATFORM="wayland;xcb"
export SDL_VIDEODRIVER=wayland
export ELECTRON_OZONE_PLATFORM_HINT=wayland
export OZONE_PLATFORM=wayland
